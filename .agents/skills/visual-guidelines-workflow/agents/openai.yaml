interface:
  display_name: "Visual Guidelines Workflow"
  short_description: "Set visual rules for the project UI."
  default_prompt: "Use this skill to define and maintain the visual rules for the project UI: colors, typography, spacing, components, states, and responsive consistency."
